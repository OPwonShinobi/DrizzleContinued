-- !!IMPORTANT!!
-- This operation will drop the whole database of yecdata and can not be reverted. 
-- Please backup all the data (if you need it) before executing this script.

USE yecdata;

-- Drop the entire database, no point dropping tables when u can do it all at once
DROP DATABASE IF EXISTS yecdata;

